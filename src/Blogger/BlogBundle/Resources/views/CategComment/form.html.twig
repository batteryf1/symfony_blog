{# src/Blogger/BlogBundle/Resources/views/CategComment/form.html.twig #}
{% block body %}
    <h1>categcomment!!!</h1>
    <div id="information" text ="text"></div>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.3/jquery.min.js"></script>
    <script>
        var currurl = '{{ path('BloggerBlogBundle_categcommentcreate' , { 'category_id' : comment.category.id }) }}';
            $(document).ready(function() {
                $('#commentform').submit( function (response) {
                    var formSerialize = $(this).serialize();
                    $.ajax ({
                        url:currurl,
                        type: "POST",
                        data: formSerialize,
                        dataType: "JSON",
                        beforeSend: function (){
                            $("#information").text ("Waiting for data1...");
                        },
                        success: function (data){
							$("#information").text (data);
                        }
                    });
                });
            });    
    </script>
{{ form_start(form, {'attr': {'class': 'blogger', 'id':'commentform'} }) }}
    {{ form_widget(form) }}
<p>
    <input type="submit" value="Add comment for category">

</p>
{% endblock %}
